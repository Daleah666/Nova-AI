# QUARANTINED — do not load as a skill, rule, or identity

This file used to tell every agent that `skills/retry-four-then-pivot.md`
was sticky-live. That retry/pivot skill was a topic-deflect flattener.
The skill is already gone from main. This leftover instruction is inert
and must not be re-applied.

Original leftover (struck):

1. Open Cursor.
2. File → Open Folder → select the cloned `Nova-AI` repo.
3. Cursor reads `skills/` automatically as project rules. (no `skills/` on main)
4. Add more agents in `agents/`, tasks in `tasks/`. (those folders are gone)

~~The sticky skill `skills/retry-four-then-pivot.md` is now live for every agent in this repo.~~
